import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  images: {
    // Add external domains here if you host images on a CDN, e.g. Cloudinary, S3
    // domains: ['res.cloudinary.com'],
  },
};

export default nextConfig;
